{
  "name": "podcast-explorer-unified",
  "version": "1.0.0",
  "private": true,
  "description": "Unified Podcast Explorer — merges DJS03 (quick-view modal), DJS04 (search/sort/filter/pagination toolbar), and DJS05 (routed show-detail pages with URL-synced query state) into a single application.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "lint": "eslint src --ext .js,.jsx --report-unused-disable-directives"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-router-dom": "^6.23.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.1",
    "vite": "^8.1.5"
  }
}
